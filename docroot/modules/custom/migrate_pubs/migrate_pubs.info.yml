type: module
name: Migrate Publications Library
description: 'Drupal 8 migration from pubs library'
package: Examples
# core: 8.x
dependencies:
  - drupal:migrate
  - drupal:menu_ui
  - drupal:path
  - migrate_plus:migrate_plus
config_devel:
  install:
    - migrate_plus.migration.iied.tags
    - migrate_plus.migration.iied_projects
    - migrate_plus.migration.pubs_projects
    - migrate_plus.migration.pubs_themes
    - migrate_plus.migration.pubs_doctypes
    - migrate_plus.migration.pubs_authors
    - migrate_plus.migration.pubs_series
    - migrate_plus.migration.pubs_tags
    - migrate_plus.migration.pubs_node
    - migrate_plus.migration_group.pubs
# Information added by Drupal.org packaging script on 2019-04-19
version: '8.x-1.2.1'
core_version_requirement: ^8.8 || ^9
project: 'migrate_pubs'
datestamp: 1555683500
