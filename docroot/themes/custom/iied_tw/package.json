{
  "private": true,
  "scripts": {
    "dev": "npx tailwindcss -i src/tailwind.pcss -o dist/tailwind.css --watch",
    "build": "npx tailwindcss -i src/tailwind.pcss -o dist/tailwind.css --minify",
    "browser-sync": "browser-sync start --config 'browser-sync.cjs' --http",
    "watch": "run-p dev browser-sync"
  },
  "dependencies": {
    "@tailwindcss/aspect-ratio": "^0.4.0",
    "browser-sync": "^3.0",
    "npm-run-all": "^4.1.5",
    "postcss": "^8.4.5",
    "tailwindcss": "^3.1"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.6",
    "@tailwindcss/typography": "^0.5.0"
  }
}
