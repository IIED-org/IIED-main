{#
/**
 * @file
 * Theme override for an 'input' #type form element.
 *
 * Available variables:
 * - attributes: A list of HTML attributes for the input element.
 * - children: Optional additional rendered elements.
 *
 * @see template_preprocess_input()
 * generalised from input--textfield--views--iied-europe-tracker.html.twig 
 */
#}
<div class='py-2'>
    <label class='relative'>
    <input{{ attributes.addClass('h-12 w-full px-4 text-gray-700 bg-white border border-gray-300 rounded-xl') }} placeholder="Search..." />
    </label>
</div>
{{ children }}
