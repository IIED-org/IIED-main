<!-- Search box with transition/close -->
<div
	x-cloak x-show="$store.showSearch" x-collapse @click.away="$store.showSearch = !$store.showSearch" @keydown.escape.window="$store.showSearch = !$store.showSearch" id="searchbox" class="transition-all duration-300 ease-in-out transform nav-search" x-ref="searchBox">

	<!-- Search box -->
	<div class="py-8 bg-stone-100">
		<div class="container">
			<div class="items-center lg:flex lg:space-x-12">
				<div class="lg:w-1/4">
					<div class="my-4 text-2xl font-headline xl:text-3xl">Search IIED</div>
				</div>
				<div class="search-page lg:w-3/4">
					<div class="pt-2">
						{{ drupal_block('views_exposed_filter_block:search-page_2', {wrapper: false}) }}
					</div>
				</div>
			</div>
		</div>
	</div>
</div>
