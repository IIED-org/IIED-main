display:
  default:
    display_title: Default
    display_options:
      fields:
        subject:
          label: කාරණය
        operations:
          label: මෙහෙයුම්
      exposed_form:
        options:
          submit_button: Filter
      filters:
        subject:
          expose:
            label: කාරණය
        langcode:
          expose:
            label: භාෂාව
  page_unapproved:
    display_options:
      fields:
        subject:
          label: කාරණය
        operations:
          label: මෙහෙයුම්
      filters:
        subject:
          expose:
            label: කාරණය
        langcode:
          expose:
            label: භාෂාව
