display:
  default:
    display_options:
      exposed_form:
        options:
          submit_button: Filter
      fields:
        filename:
          label: Tên
        status:
          label: 'Trạng thái'
      filters:
        status:
          expose:
            label: 'Trạng thái'
