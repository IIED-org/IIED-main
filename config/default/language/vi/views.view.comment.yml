display:
  default:
    display_title: 'Mặc định'
    display_options:
      fields:
        subject:
          label: 'Tiêu đề'
        name:
          label: 'Tác giả'
        operations:
          label: 'Thao tác'
      exposed_form:
        options:
          submit_button: Filter
      filters:
        subject:
          expose:
            label: 'Tiêu đề'
        langcode:
          expose:
            label: 'Ngôn ngữ'
  page_unapproved:
    display_options:
      fields:
        subject:
          label: 'Tiêu đề'
        name:
          label: 'Tác giả'
        operations:
          label: 'Thao tác'
      filters:
        subject:
          expose:
            label: 'Tiêu đề'
        langcode:
          expose:
            label: 'Ngôn ngữ'
